declare namespace Express {
    export interface Request{
        userId: string
    }
}
//lo que digo aca que cada vez que utilice EXPRESS voy a expandir express con una nueva propiedad en este casa userId,(para porder utilizarlo con (req.UserId) 